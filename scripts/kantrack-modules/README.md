# scripts/kantrack-modules/

Every feature module of the KanTrack application. Each file has one responsibility.

---

## TypeScript core (typed infrastructure)

| File            | What it does                                                                                                                                               |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `types.ts`      | Shared domain types: `Task`, `Column`, `Priority`, `Tag`, `NotebookItem` (incl. `expanded?: boolean`), `Clock`, `UndoEntry`                                |
| `store.ts`      | Minimal flux-like store — `dispatch()`, `subscribe()`, `getState()`, typed reducer, `TASK_*` action types                                                  |
| `router.ts`     | Event delegation router — `registerAction()` maps `data-action` names to handlers; `initRouter()` attaches one document-level click listener               |
| `repository.ts` | **All** persistence logic: `getAllTasks()`, `saveTasks()`, `getAllNotebookItems()`, etc. Writes localStorage synchronously and IDB async (fire-and-forget) |

---

## JavaScript feature modules

| File                 | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `clocks.js`          | World clock panel: add/remove timezone clocks and chronometers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `context-menu.js`    | Generic floating context-menu utility used by priority and timer pickers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `database.js`        | Raw IndexedDB setup, schema migration runner, image CRUD helpers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `drag-drop.js`       | Drag-and-drop between kanban columns; touch drag support                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `due-dates.js`       | Due date picker render, overdue/today status detection and card CSS classes; soft-deleted tasks are excluded from overdue/due-today/due-soon queries                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `export.js`          | Export a single task as PDF (uses jsPDF)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `images.js`          | Image modal viewer; clipboard paste-to-image handler                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `loading.js`         | Loading overlay and progress indicator utilities                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `mentions.js`        | Disabled @mention system — kept as no-ops for backward compatibility                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `modal.js`           | Task detail modal: `openTaskModal()`, `saveModal()` (save without closing), `saveAndCloseModal()`, `closeTaskModal()`, note history render; shared save logic extracted into `_persistModalChanges()`; images restored in parallel via `Promise.all`; refreshes tag filter bar on close                                                                                                                                                                                                                                                                                                                                |
| `notebook-export.js` | Export notebook pages as PDF or entire notebook as ZIP (uses jsPDF + JSZip)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `notebook.js`        | Notebook sidebar: page/folder tree, page editor modal, import/export; touch event handler uses string IDs (not `parseInt`) so folder expand/collapse works correctly on touch devices                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `notifications.js`   | `showError()` / `showWarning()` / `showInfo()` toast notifications                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `priority.js`        | Priority logic: `setModalPriority()`, `updateNoteCardPriority()`, quick priority context menu                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `search.js`          | Search bar and tag/column filter overlay; tag filter matches by tag ID (not name); `initSearch()` attaches a single delegated listener on the `#tagFilters` container so clicks work correctly even after `renderTagFilterButtons()` re-renders the DOM                                                                                                                                                                                                                                                                                                                                                                |
| `sorting.js`         | Priority-based sort within a kanban column (`sortColumnByPriority()`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `state.js`           | Shared mutable state object — 27 values + 44 setters. The central source of truth for in-memory app state                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `storage.js`         | Thin shim: wraps `repository.ts` calls with `state.js` interaction. Provides `loadNotesFromLocalStorage()` and `saveNotesToLocalStorage()`                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `sanitize.js`        | Allowlist-based HTML sanitizer (`sanitizeHTML()`); applied to all stored HTML before it enters the live DOM                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `storage-monitor.js` | Requests durable storage, monitors quota, dispatches warning notifications                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `sub-kanban.js`      | Nested sub-kanban board inside the task modal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `tags.js`            | Tag creation, assignment, color coding (preset palette + custom hex); the custom color `<input type="color">` carries class `.tag-color-input-hidden` (zero-sized, `pointer-events: none`) and is opened programmatically via a separate button's `click()` call to prevent browser minimum-size overflow covering adjacent UI; pinned tags show in a management panel inside the task modal with assign/delete actions; `renderTagFilterButtons()` populates the `#tagFilters` bar with the 8 most-used tags by frequency across active (non-deleted) tasks; `cleanupUnusedTags` preserves tags used by trashed tasks |
| `tasks.js`           | Core task CRUD: `addNote()`, `deleteNote()`, `createNoteElement()`, `updateNoteCardDisplay()`; cards have `tabIndex=0` for keyboard nav; deletion records history entry before cloning to trash                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `timer.js`           | Task timer: add/remove time, quick-time context menu, long-press support                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `timezones.js`       | Static timezone data list used by the clock system                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `undo.js`            | Undo/redo system: `recordAction()`, `undo()`, `redo()`, trash integration; `permanentlyDelete` and `emptyTrash` also remove entries from `state.notesData` to prevent stale state                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `utils.js`           | Pure utilities: `formatTime()`, `escapeHtml()`, `deepClone()`, `validateTitle()`, `createFocusTrap()`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `virtual-list.js`    | Virtualised card renderer — `VirtualList` class with `IntersectionObserver`; renders only the visible cards in each column                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

---

## Key data flows

### Creating a task

`addNote()` (tasks.js) → `state.pushToNotesData()` → `saveNotesToLocalStorage()` → `repository.saveTasks()` → localStorage (sync) + IDB (async)

### Saving modal changes

Both paths share `_persistModalChanges()` which handles title, priority, tags, timer, and note content:

- **Save & Close:** `saveAndCloseModal()` → `_persistModalChanges()` → `saveNotesToLocalStorage()` → `updateNoteCardDisplay()` → `closeTaskModal()`
- **Save (keep open):** `saveModal()` → `_persistModalChanges()` → `saveNotesToLocalStorage()` → `updateNoteCardDisplay()` → resets tracking state + clears editor + re-renders history in place

### Loading on startup

`loadNotesFromLocalStorage()` (storage.js) → `repository.getAllTasks()` → localStorage (primary, always written synchronously) or IDB (fallback) → `state.setNotesData()` → render all cards

---

## Notable bug fixes

### Tag filter buttons did nothing after a page reload (`search.js` + `tags.js`)

After a full browser reload, clicking a tag filter button had no visible effect — no tasks were filtered and the button never turned active.

**Root cause — duplicate event listeners**

The bootstrap sequence ran two listener-attachment paths over the same DOM nodes:

1. `renderTagFilterButtons()` — rendered the buttons and attached a dynamic-import listener to each: `import('./search.js').then(m => m.toggleTagFilter(tag))`
2. `initSearch()` — queried those same buttons and attached a second, synchronous listener

On **first load** (no stored data) `renderTagFilterButtons()` showed the "no tags" message, so `initSearch()` found no buttons. Tags added later in the same session only ever got the single dynamic-import listener. On **reload** (data already in storage) both ran in sequence, and each button ended up with two listeners.

On click, the synchronous listener fired and added the tag to `currentTagFilter`; then the dynamic-import `.then()` ran as a microtask (before any repaint) and toggled the same tag back off. The user always saw only the final state — no filter applied.

**Fix**

- `initSearch()` — replaced the per-button `querySelectorAll` loop with a single delegated listener on the `#tagFilters` container. The container is permanent; its buttons are replaced dynamically. Delegation handles any child button regardless of when it was created.
- `renderTagFilterButtons()` — removed the per-button listener attachment block entirely. Event wiring is now exclusively the responsibility of `initSearch()`.

---

### PDF export silently dropped images on macOS (`export.js`)

When exporting a task to PDF on macOS, the resulting file contained no images even though the same workflow worked correctly on Windows.

**Root cause — two compounding bugs**

1. **Shallow `childNodes` traversal** — `exportTaskAsPDF` iterated only the direct children of the temporary DOM element. On macOS, Chrome's `contenteditable` wraps pasted content in `<div>` block elements, so `<img>` nodes were nested one level deeper and never reached.

2. **Hardcoded `'PNG'` format in `doc.addImage`** — macOS clipboard (Preview, Keynote, etc.) puts `image/tiff` data URLs on the clipboard. Passing a TIFF data URL with the format forced to `'PNG'` causes jsPDF to fail silently inside its own `try/catch`.

**Fix**

Two private helpers were added before `exportTaskAsPDF`:

- `_normalizeImageToPng(dataUrl)` — converts any image format to PNG via `<canvas>` before passing to jsPDF; PNG and JPEG pass through unchanged.
- `_walkNodesForPDF(nodes, doc, taskId, margin, maxWidth, yPosRef)` — recursively walks a `NodeList`, handling text nodes, `<img>` elements (IDB lookup → `data-src` fallback → `src` fallback, all normalised through `_normalizeImageToPng`), and any other element by descending into its `childNodes`.

The flat `childNodes` loop in `exportTaskAsPDF` was replaced with a call to `_walkNodesForPDF`. Only `exportTaskAsPDF` was affected — the JSON/encrypted export paths use a separate `querySelectorAll('img')` (recursive) approach and were never broken.
